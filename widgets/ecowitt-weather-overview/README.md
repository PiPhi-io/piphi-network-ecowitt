# Ecowitt weather overview

Read-only PiPhi dashboard widget for live Ecowitt weather, rain, wind, solar,
air-quality, lightning, and accessory-sensor values. It uses only the injected
Widget SDK host and requests no browser or command permissions.

Run `npm install`, `npm run validate`, `npm run conformance`, then `npm run dev`.
